configfile: 'config.yaml'

include: 'rules/get-sample-list.smk'
include: 'rules/hgsvc-qc.smk'
include: 'rules/compare-vcf.smk'
include: 'rules/vamos.smk'
include: 'rules/analysis.smk'

comaprison_samples = ['HG03499','HG03548']
afs = ['0.1', '0.5', '1']
haplotypes = ['hap1', 'hap2']

wildcard_constraints:
    sample='|'.join(t2t_samples),
    af='|'.join(afs),
    haplotype='|'.join(haplotypes)

rule all:
    input:
        #expand('results/miller-comparison/vamos-results-vienna/{sample}.vcf', sample=samples),
        #expand('results/miller-comparison/plots/distance-histogram/{sample}-{af}-subset1000.png', sample=samples, af=afs),
        #expand('results/miller-comparison/plots/distance-histogram/{sample}-{af}-full.png', sample=samples, af=afs),
        #expand('results/miller-comparison/plots/distance-histogram/HG03499_HG03548-{af}-subset1000.png', af=afs),
        #expand('results/miller-comparison/plots/distance-histogram/HG03548_HG03499-{af}-subset1000.png', af=afs),
        #expand('results/miller-comparison/plots/distance-histogram/HG03499_HG03548-{af}-full.png', af=afs),
        #expand('results/miller-comparison/plots/distance-histogram/HG03548_HG03499-{af}-full.png', af=afs)
        #expand('results/miller-comparison/callset-comparison/intersection/{sample}.tsv', sample=comaprison_samples),
        #expand('results/miller-comparison/callset-comparison/intersection/upsetplot_{sample}.pdf', sample=comaprison_samples),
        #expand('results/vamos-t2t/{sample}.vcf', sample=t2t_samples),
        #'results/vamos-t2t-summary.bed',
        expand('results/hgsvc3-comparison/vntr-calls/{sample}-{haplotype}.vcf', sample=hgsvc_samples, haplotype=haplotypes),
        'results/hgsvc3-comparison/qc/ru-boxplot-pcc.svg'